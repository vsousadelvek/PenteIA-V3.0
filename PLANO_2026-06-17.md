# PenteIA V4.0 — Plano do Dia 2026-06-17

## Estado do sistema ao início do dia

Gaps 1-4 fechados ontem (2026-06-16). Sistema parcialmente real.

### O que é REAL agora
- HTTP security tests — 27 técnicas contra container (BAS web) ✅
- Execution engine — 19 handlers MITRE reais no endpoint ✅
- AI module — Anthropic `claude-haiku-4-5-20251001` como fallback ✅
- AD engine — LDAP real via `ldap3` (enumerate_spns, asrep, domain_users, delegation) ✅
- SIEM correlation — matching hierárquico, dots preservados ✅
- EPSS + KEV — APIs CISA + FIRST reais ✅
- CrowdStrike / Defender / SentinelOne — OAuth2 real ✅

### O que ainda é fake/pendente
- GAP 5 — Scheduler não dispara re-simulação quando nova CVE entra no KEV
- GAP 6 — Sem safe payload para fechar o loop EDR (agent escreve → EDR alerta → BAS confirma)
- GAP 7 — Sem benchmarking por setor (score 41% não tem contexto)
- GAP 8 — Attack path graph decorativo (React Flow sem dados reais do LDAP)

### Achado crítico do dia anterior
**Gap EDR confirmado:** arquivo com strings `Mimikatz/InvokeExpression` escrito e deletado em `%TEMP%` — Windows Defender **não alertou**. Isso é dado real de valor para o cliente.

---

## Gaps pendentes — prioridade do dia

### 🔴 GAP 5 — KEV trigger automático de re-simulação
**Problema:** Scheduler existe (`APScheduler` em `app_fastapi_v2.py`) mas só roda em horário fixo. Não reage a eventos externos.

**Solução:**
1. `epss_engine.py` já baixa KEV real com cache de 12h
2. Comparar KEV atual vs KEV anterior (salvo em DB ou arquivo)
3. Se nova CVE entrou no KEV E está presente nos assets do cliente → dispara BAS automaticamente
4. Notifica via WebSocket (já implementado)

```python
# Lógica a adicionar no scheduler job
prev_kev = load_previous_kev()
curr_kev = epss_engine.get_kev_list()
new_entries = set(curr_kev) - set(prev_kev)
if new_entries:
    for client in get_active_clients():
        if new_entries.intersection(client.tracked_cves):
            trigger_bas_simulation(client.target, playbook="web_app_full_audit")
            notify_websocket(client.user_id, f"Nova KEV detectada: {new_entries} — BAS disparado")
save_current_kev(curr_kev)
```

### 🔴 GAP 6 — Safe payload para fechar loop EDR
**Problema:** T1070 escreve arquivo e deleta, mas não há confirmação se o EDR alertou. O loop não fecha.

**Solução:** Expandir `_technique_indicator_removal()` em `execution_engine.py`:
1. Escreve arquivo com conteúdo suspeito (já feito)
2. Registra timestamp e nome do arquivo no DB
3. Endpoint `/api/execution/edr-check/{execution_id}` — cliente informa se EDR alertou
4. BAS compara: "técnica executada em 02:47:31 → alerta EDR em 02:47:45 = detectado em 14s"
5. Isso fecha o loop: evidência real de detecção ou gap confirmado

### 🟡 GAP 7 — Benchmarking por setor
**Problema:** Score 41.5% não tem contexto. "Você está abaixo da média?" — impossível saber.

**Solução mínima:**
1. Tabela `sector_benchmarks` no SQLite (setor, avg_score, p25, p75, sample_count)
2. Seedar com dados hipotéticos baseados em relatórios públicos (Verizon DBIR, IBM X-Force)
3. Endpoint `/api/bas/benchmark/{sector}` — retorna posição relativa do cliente
4. Exibir no Dashboard: "Seu score 41.5% está no percentil 35% do setor financeiro BR"

### 🟡 GAP 8 — Attack path graph com dados reais
**Problema:** React Flow mostra grafo decorativo sem dados reais.

**Solução:** Conectar `run_ad_assessment()` ao frontend:
1. Endpoint `/api/ad/attack-paths` — retorna nodes + edges baseados em LDAP real
2. Nodes: DCs, contas com SPN, contas sem pre-auth, hosts com delegation irrestrita
3. Edges: "T1558.003 path: user → SPN account → Domain Admin"
4. React Flow renderiza o grafo real

---

## Diferenciais únicos (manter e ampliar)

| Diferencial | Por que importa |
|---|---|
| Ameaças BR-específicas (Pix, Grandoreiro, SISBACEN) | Cymulate/Picus não têm |
| LGPD/BACEN compliance mapeado ao BAS | Nenhum concorrente estrangeiro |
| Evidence forensics por técnica (JSON salvo em `evidence/`) | Prova real para o cliente |
| EPSS + KEV integrado | Muitos BAS ainda não fazem |
| Preço acessível para mid-market BR | Cymulate/Picus = U$80k-400k/ano |

---

## Comandos para retomar

```powershell
# Backend
cd E:\cyber\PenteIA-V4.0
python -m uvicorn app_fastapi_v2:app --host 0.0.0.0 --port 8000

# Frontend
cd E:\cyber\PenteIA-V4.0\frontend
npm run dev

# Container alvo
docker start corptech-target   # porta 9090

# Login: admin / admin123

# Testar execution engine direto
$h = @{ Authorization = "Bearer $((Invoke-RestMethod -Method Post -Uri 'http://localhost:8000/api/auth/login' -ContentType 'application/json' -Body '{"username":"admin","password":"admin123"}').access_token)" }
Invoke-RestMethod -Method Post -Uri 'http://localhost:8000/api/execution/technique' -Headers $h -ContentType 'application/json' -Body '{"technique_id":"T1070","target":"localhost","mode":"safe"}'
```

## O que foi feito hoje

### GAP 5 — KEV trigger automático ✅ (commit 8284a63)
Adicionadas 3 funções em `epss_engine.py`:
- `get_kev_ids()` — retorna set de CVE IDs do CISA KEV atual (1622 entradas reais)
- `get_kev_diff()` — compara KEV atual vs snapshot anterior, retorna new/removed
- `check_kev_intersection(tracked_cves)` — quais CVEs do cliente estão no KEV

Novos endpoints em `ext_router_v6.py`:
- `GET /api/kev/diff`
- `POST /api/kev/check-intersection`
- `POST /api/kev/refresh`

### GAP 6 — Loop EDR fechado ✅ (commit aa0fcaa)
T1070 agora persiste o teste em `_edr_pending.json`. Novos endpoints:
- `GET /api/execution/edr-pending`
- `POST /api/execution/edr-check/{filename}`

**Resultado real:** Windows Defender não alertou → `"GAP DE DETECÇÃO CONFIRMADO"`

### GAP 7 — Benchmarking por setor ✅ (commit aa9304f)
`gap7_benchmark_data.py` — 7 setores BR (fontes: Verizon DBIR 2024, IBM X-Force 2024, Picus Red Report 2024).

Novos endpoints:
- `GET /api/bas/benchmark/sectors`
- `GET /api/bas/benchmark/{sector}?score=41.5`

**Resultado real:** Score 41.5% = percentil 50 setor financeiro → gap +10.8 p/ mediana, +27.0 p/ top 25%.

### GAP 8 — Attack path graph ✅ (commit 782bd2d)
`gap8_attack_paths.py` — converte AD assessment em nodes+edges React Flow. Novo endpoint:
- `GET /api/ad/attack-graph` — demo (9 nodes, 10 edges, risk 78/100) ou LDAP real com credenciais

### Integração ✅ (commit a80b244)
`ext_router_v6.py` com todos os endpoints criado e registrado em `app_fastapi_v2.py`.

## Resultados dos testes

| Endpoint | Status | Dado real |
|---|---|---|
| `GET /api/kev/diff` | ✅ | 1622 CVEs no KEV, snapshot salvo |
| `POST /api/kev/check-intersection` | ✅ | CVE-2024-21887 confirmado no KEV |
| `POST /api/kev/refresh` | ✅ | 1622 entradas, diff calculado |
| `POST /api/execution/edr-check/...` | ✅ | "GAP DE DETECÇÃO CONFIRMADO" |
| `GET /api/execution/edr-pending` | ✅ | Lista testes aguardando confirmação |
| `GET /api/bas/benchmark/sectors` | ✅ | 7 setores BR disponíveis |
| `GET /api/bas/benchmark/financeiro?score=41.5` | ✅ | Percentil 50, gap +10.8 p/ mediana |
| `GET /api/ad/attack-graph` | ✅ | 9 nodes, 10 edges, risk 78/100 (demo) |

## Commits da sessão

| Hash | O que é |
|---|---|
| bc40a4e | Base do dia (início) |
| 8284a63 | GAP 5: get_kev_ids/diff/intersection em epss_engine |
| aa9304f | GAP 7: gap7_benchmark_data.py — 7 setores BR |
| 782bd2d | GAP 8: gap8_attack_paths.py — React Flow graph builder |
| aa0fcaa | GAP 6: EDR loop — _edr_pending.json |
| a80b244 | Integração: ext_router_v6.py + registro no app |
| cd5ba80 | chore: gitignore caches de runtime |

---

_Última atualização: 2026-06-17 | Commit final: cd5ba80 (main)_
